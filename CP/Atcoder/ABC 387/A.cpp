#include <bits/stdc++.h>
#define int long long

#define MAX 200100
using namespace std;

signed main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0), cout.tie(0);

    int A, B;
    cin >> A >> B;
    cout << (A + B) * (A + B) << '\n';

    return 0;
}