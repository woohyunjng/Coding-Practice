#include <bits/stdc++.h>
#define int long long

#define MAX 200100
using namespace std;

void solve() {
    int N;
}

signed main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0), cout.tie(0);

    int T;
    cin >> T;

    while (T--)
        solve();

    return 0;
}