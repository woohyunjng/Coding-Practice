#include <bits/stdc++.h>
using namespace std;

signed main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0), cout.tie(0);

    string S;
    int N;

    cin >> S, N = S.size();
    for (int i = 0; i < N; i++)
        cout << i << '\n';

    return 0;
}